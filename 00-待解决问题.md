# 待解决的问题

> 王楠 2026-09-02 原话，未改写。
> 项目索引见 `00-项目总入口.md`。

---

1. 竞品和关键词页面的设计有问题，在架构上完全依赖 agent 的输出，而不是向库存和广告一样一部分直接加载原始数据，一部分依赖 agent 判断

2. 数据库不干净的问题，原始数据库应该只包含已发生和已定义信息，未发生和未推断结论不能存在

3. agent 问题
   （1）是否仍然有 agent 读未发生和未推断结论当做已知条件
   （2）agent loop 时间长，展示问题

4. 工作台前端的数据接口和 agent 接口没有清晰文档的问题。需要梳理整体的

   工作台读取数据范围
   agent 读取数据范围
   agent 写入数据范围
   工作台读取 agent 写入的格式范围规范

5. 关键词和竞品的观察库里混着一批已经没人读的 v1 时代 Agent 判断表，让包看起来像「依赖 Agent 输出」，而真实情况相反——这两个模块的 Agent 都还没成功落过表，页面现在全靠原始事实加规则算子顶着。

---

这是我总结的 5 个问题 大多数都是工作台和 agent 协作的逻辑没有梳理清楚

---

## 补充诊断：关键词 Agent Loop 架构异常

> 以下内容不是王楠原话，是对当前实现的技术现象、执行链路和设计失误的整理，供其他模型复核。

### 一、问题现象

当前关键词模块存在两条彼此割裂的运行链路：

1. **前端 Loop Smoke**
   - 入口实现：`06-Pi-Agent交互Demo/src/keyword-loop-task.ts`
   - 用户点击分析后，会冻结参数、读取少量市场候选、启动 Pi 会话，让模型判断一批数据并把步骤状态返回前端。
   - 这条链路能够证明“真实 Pi Agent 已启动”和“前端可以展示执行过程”。
   - 但它只执行一个局部阶段，不继续执行覆盖变化、证据判断、日报、盘点和正式发布，因此不会生成页面最终需要的完整结果。

2. **正式结果生产 Loop**
   - 入口实现：`06-Pi-Agent交互Demo/scripts/run_keyword_agent_demo.ts`
   - 执行顺序为：事实桥接 → C 市场变化 → D 覆盖变化 → A 证据判断 → B 日报 → E 子 ASIN 盘点 → 校验 → 发布。
   - 这条链路理论上可以产生完整结果，但当前把大批数据拆成大量模型调用，并且全部串行执行，导致运行时间不可接受、失败概率很高。

因此目前的实际状态是：

> 前端能展示过程的 Loop 不产出正式答案；能产出正式答案的 Loop 又无法在合理时间内完成。两条链路不是同一个任务链。

### 二、正式 Loop 当前如何一步一步得到答案

当前实现不是由 Agent 自主规划分析步骤，而是代码预先写死了完整阶段和批次数量：

```text
原始事实表
  ↓
程序生成全量候选项
  ↓
C 市场变化：约 600 条候选，50 条/批，约 12 次模型调用
  ↓
D 覆盖变化：约 1802 条候选，50 条/批，约 37 次模型调用
  ↓
A 证据判断：约 901 条候选，20 条/批，约 46 次模型调用
  ↓
B 生成 14 天日报：约 1 次模型调用
  ↓
E 子 ASIN 盘点：约 1 次模型调用
  ↓
提交/发布：约 1 次模型调用
```

全程合计约 **98 次串行模型调用**。每一批通常新建独立 Pi 会话，只暴露当前阶段使用的工具；模型必须为该批次的每个 `candidate_id` 返回一条判断，即使判断结果为空，也必须返回完整覆盖。

单次模型调用默认最长可等待约 180 秒，并允许重试。即使平均每次只花 20 秒，纯模型调用时间也已超过 30 分钟；任何超时或重试都会把总耗时进一步放大。

这意味着它本质上是：

> 一条由代码写死阶段的批处理工作流，内部使用大模型逐行充当分类器，而不是一个根据阶段结果决定下一步行动的轻量 Agent Loop。

### 三、已经观察到的失败状态

半成品恢复库：

`09-工作台/modules/keyword/derived/runs/keyword-all-2026-08-03-003.recovery.sqlite`

其中已经写入：

| 阶段 | 已有行数 |
|---|---:|
| C 市场变化 | 85 |
| D 覆盖变化 | 45 |
| A 证据判断 | 58 |
| B 日报 | 0 |
| E 盘点 | 0 |

manifest 仍为 `writing`，`completed_at` 为空，正式发布文件 `keyword_agent_current.sqlite` 不存在。这个状态说明任务在完成 A 阶段以前或过程中退出，控制流根本没有到达 B、E 和发布阶段；B、E 为 0 不一定代表其自身逻辑报错。

前端 Loop Smoke 状态库：

`09-工作台/modules/keyword/derived/keyword_agent_loop_state.sqlite`

已观察到多次运行都在模型判断步骤失败，错误为 `MODEL_TIMEOUT`，约 61 秒后结束，接收结果数为 0。

### 四、为什么会设计成这样

这不是 Pi 框架的限制，而是当前实现的设计错误。主要原因如下：

1. **把“最终结果表必须完整”错误地理解成“每一行都必须由模型亲自判断”。**
2. **把“Agent 有多个有意义的分析阶段”错误地实现成“把全量数据拆成很多技术批次”。**
3. **让模型参与确定性工作。** 汇总、校验、落库和发布本应由程序直接完成，却也被包装成模型工具调用。
4. **优先保证数据契约的机械覆盖，没有优先控制一次 Agent 任务的分析范围、耗时和失败面。**
5. **Demo 目标收缩后没有同步重构主链路。** 后来只要求前端能看到真实 Loop 启动，但实现是在旧的全量 Runner 旁边另加一条 Smoke 链路，最终造成展示链与结果链分离。

直接责任结论：

> 当前方案把 Agent 错误地设计成了昂贵的逐行 ETL/分类工具。数据量和模型速度会放大问题，但不是根因；根因是任务拆分与控制流设计错误。

### 五、期望其他模型重点复核的问题

1. 关键词模块是否应该先拆成“关键词市场分析 Agent”和“ASIN 关键词分析 Agent”，而不是一次默认跑全部对象和全部任务？
2. 哪些指标可以由 SQL/确定性程序全量计算，哪些少量异常或歧义项才真正需要模型判断？
3. 如何把正式链路压缩为约 4～6 个有业务意义的 Agent 阶段，而不是约 98 次技术批次？
4. 前端进度事件如何直接来自正式任务，使“页面看到的 Loop”与“最终落库的 Loop”成为同一条链？
5. 中途停止时，是否允许保留并展示阶段进度，但仍严格禁止把半份结果当作正式结论发布？
6. B 日报、E 盘点、校验和发布中，哪些应完全移出模型循环，改成确定性程序步骤？

### 六、建议的目标形态（供讨论，不代表已经实施）

```text
用户先选择分析对象和范围
  ↓
程序读取真实历史事实并计算全量基础指标
  ↓
规则筛出少量异常、冲突和不确定项
  ↓
Agent 判断市场变化
  ↓
Agent 判断覆盖变化和关键证据
  ↓
Agent 综合形成结论与建议
  ↓
程序完成汇总、契约校验、原子落库与发布
```

目标不是减少页面上显示的步骤，而是让每一步都代表真实的业务判断；全量计算和机械性工作交给程序，模型只处理必须依靠语义理解与综合判断的部分。
