# 广告 Agent v2 前端接缝实跑

## 可用于联调的真实运行

- 主体：`B0B3LWGP36`
- run_id：`B0B3LWGP36-2026-08-03-ads-agent-20260831120619-77e8bb`
- 状态：`completed`
- 来源：`Pi Agent`
- 模型：`deepseek/deepseek-v4-flash`
- freshness：`5ececadce7d57c0936576a94489e3dea21ebf137bfe5221c255f97ec0bd13c1f`
- context_id：`dec_B0B3LWGP36_20260803@5ececadce7d5`
- data_as_of：`2026-08-03`

`GET /api/ads/run?child_asin=B0B3LWGP36` 已返回 `condition=正常`、`source=Pi Agent`、`context_hash_matched=true`。八个输出点已经存在：

| output_point | 条数 |
|---|---:|
| B0b | 4 |
| B0cd | 13 |
| B0e | 4 |
| B1 | 4 |
| B2 | 4 |
| B3 | 6 |
| B3b | 10 |
| B4 | 6 |

这条运行可验证 API 形状和前端注入，但它产出后才新增两条语义门禁；其中 `REQUIRED_TASK_MISSING` 把“任务已有但缺上游输入”误归为“任务未建立”。不要把这条旧诊断当最终业务样例。后续新运行会拒绝此矛盾。

## 前端当前需要改的接缝

运行字段对账结果为“服务端吃掉 0、白写 1、会空 4”，不是 Agent 少落字段，而是读取层仍混用旧 payload：

1. task、diagnosis、proposal：读取 `judgment_mode`，不要继续要求旧 `mode`。
2. mapping：Agent 给 `ad_object_id`；如页面需要完整 `ad_object`，由读取层用目录补全，不要要求 Agent 复制对象展示数据。
3. proposal：读取并传递 `evidence_ids`，否则依据 chip 白写。
4. 新增 B0cd、B0e、B3b 的明确读取函数；当前检查脚本能看到接口字段，但找不到前端消费者。
5. `*_label`、对象名和枚举中文都由前端映射，Agent 不补中文译名字段。

完成这五项后再运行：

```bash
cd /Users/linsen/BAM/09-工作台
/usr/bin/python3 tests/check_agent_field_contract.py
```

目标仍是三个差异数全部为 0。

## Runner 命令与当前模型状态

默认命令：

```bash
cd /Users/linsen/BAM/06-Pi-Agent交互Demo
npm run demo:ads-agent -- B0B3LWGP36
```

临时选择 Pi 已配置模型，不修改全局默认：

```bash
BAMBOO_ADS_MODEL='provider/model-id' npm run demo:ads-agent -- B0B3LWGP36
```

2026-08-31 复跑时，默认 DeepSeek 通道返回 `402 Insufficient Balance`；两个 xiaoyao 备用通道分别返回无可用 channel、上游失败或不调用工具。因此新的严格门禁代码已通过测试，但尚未产出第二条 completed 运行。失败尝试均发生在新建 run 前，没有半份结果落库。
