# Bamboocool AI 运营工作台 · Agent 工作规则

**本文件适用于 `/Users/linsen/BAM` 及其全部子目录。**

这个仓库就是项目本身。2026-09-01 重构过一次：原来它是资料库、真项目埋在
`06-Bamboocool-Demo重建/` 里；现在那一层前缀去掉了，与新 demo 无关的东西移出到
`/Users/linsen/BAM-归档/`。如果你在任何文档里看到 `06-Bamboocool-Demo重建/` 这个路径段，
那是漏改的残留，去掉它就是现在的正确路径。

> **先读 `00-项目总入口.md`**（全项目索引）**与 `00-待解决问题.md`**。
>
> `00-项目总入口.md` 那份是全项目索引：按问题找文档、数据用哪一版、
> 九个 Agent 的真实状态、十个已知陷阱、等王楠拍的决定。本文件只讲「怎么动手」。

---

## 一、跑起来的只有两个进程

```
工作台     18820   09-工作台/          Python 3.9 stdlib 零依赖，只绑 127.0.0.1、无鉴权
Agent 侧   18812   06-Pi-Agent交互Demo/  node + TS，真调 deepseek/deepseek-v4-flash
```

启停必须 `/usr/bin/python3 09-工作台/start.py {start|stop|restart|status}`。
**不要用 `python3`**（那是 homebrew 的 3.14，依赖不同）。

浏览器只跟 18820 说话，`/api/agent/*` 由 `server.py:_proxy_agent` 转发到 18812。

**`09-工作台/` 与 `06-Pi-Agent交互Demo/` 必须保持同级。** Agent 侧用
`resolve(PROJECT_ROOT, "..", "09-工作台")` 定位工作台，Agent 写的库落在工作台自己的
`modules/*/derived/` 里——这个相对关系一破，整条接缝就断。

---

## 二、视觉规范（这一条以前被写错过）

**不采用 Organic Soft-Tech。** 它是上一代（V5）的视觉体系，已随 V5 前端一起移进
`/Users/linsen/BAM-归档/`。归档里那份 `AGENTS.md-原版` 仍写着「以 Organic Soft-Tech 为基线」——
那句话对归档里的旧前端成立，**对本项目不成立**。

现行视觉基准：

- `00-通用方法与规范/06-视觉基准要则.md` + 同目录两张 png（设计图与参考实现）
- 字号只准用 `09-工作台/web/tokens.css` 的九档令牌，**CSS 里不许出现 font-size 数字字面量**，
  JS 里不许写内联 font-size
- **不许用 `var(--mono)` 等宽字体**；要数字列对齐就 `font-variant-numeric: tabular-nums` 配 `var(--sans)`
- 语义色只用现成四档：`--alert` 红 / `--warn` 橙 / `--good` 绿 / `--calm` 紫，不新增颜色
- 玻璃材质那几个 `--glass-*` 是王楠用 `?tune=1` 调参器定的，**想改就开调参器，不要手改 tokens.css**

这些有机械门禁盯着（G19–G24），违反会红。

---

## 三、动手前必读

| 你要做什么 | 先读 |
|---|---|
| 加/改一个业务模块 | `00-通用方法与规范/02-模块接入契约.md` |
| 给 Agent 写需求或数据契约 | `00-通用方法与规范/08-Agent需求与数据契约要则.md` |
| 让 Agent 落库 | `00-通用方法与规范/07-Agent运行与落库口径.md` |
| 改页面视觉 | `00-通用方法与规范/06-视觉基准要则.md` |
| 决定某个判断该怎么上屏 | `11-页面框架与决策/Bamboocool-工作台全局信息设计原则.md` ← **优先级最高，模块接入契约自己写「冲突时以它为准」** |
| 查某个模块该算出什么 | `10-产品方案/Bamboocool-<模块>模块必备逻辑与能力清单.md` |
| 查为什么这么设计 | `12-会议与访谈/`（9 份逐字稿；**2026-08-31 那两份是最新决定**） |

**门禁清单被劈成两半，两边都不全**：契约 §12 停在 G17，而实际在跑的是 24 条——
多出来的 G18–G24 是视觉基准那几条，写在 `06-视觉基准要则.md` 里。
跑 `/usr/bin/python3 09-工作台/tests/test_gates.py` 才是真的。

---

## 四、硬约束

**写库全在 Agent 侧。** 工作台永不写库（契约 6.1，`core/db.py` 额外加 `PRAGMA query_only`）。
模块要数据就只读打开，页面发请求轮询状态。

**模块里不要改外壳文件**：`09-工作台/core/`、`web/shell.js`、`web/tokens.css`、
`web/base.css`、`web/index.html`。加模块是外壳零改动的——`core/registry.py` 扫
`modules/*/module.py` 自注册。

**`06-Pi-Agent交互Demo/` 是王楠主导的项目。** 回答问题、给写法、改他点名的段落可以；
动手实现、发明表结构写进他的契约，**必须先要到授权**。

**给 Agent 的输入只能是「Agent 跑之前真实业务里就存在的东西」**——已发生的事实、
产品关系、人排的计划。任何预测和任何已有判断都不能进输入（断货日、覆盖天数、风险等级、
置信度、预期提升系数、生命周期阶段……那些本该是 Agent 的产出）。
判据一句话：**这条数据在真实业务的后台里存在吗。**

**界面上不写防御性自述。** 「不作为判断依据」「阈值待客户确认」这类是给开发者的解释，
客户不需要。方法与口径收进 ⓘ 浮层，不写成正文一行字。英文码值（`derived`、`can_absorb`）
一律不许上屏。

---

## 五、五个已经踩过的坑

**一、`09-工作台/modules/keyword/` 会遮蔽 Python 标准库的 `keyword`。**
在 `modules/` 目录里跑任何 python 脚本都会炸在 `from keyword import iskeyword`，
报错跟你的代码毫无关系。**别 cd 进 `modules/` 跑 python**，从 `09-工作台/` 跑。

**二、时间戳有三种格式并存**（`+08:00` / `.Z` / 无偏移）。取「最新」**必须解析成时刻再比**，
不能 `ORDER BY created_at DESC`——带 `Z` 的会被判成前一天，更新的记录被更旧的盖掉，
而且界面上完全看不出异常。正确写法看 `09-工作台/modules/agentcfg/module.py._stamp`。
**这个坑修过两次，至今没有门禁挡回归。**

**三、库存 `v0.2.2` 不是旧版，它和 `v0.3.0` 同时在用**（`modules/inventory/data.py`
把它当来源事实包读，页面要的四个质量汇总块只在它里面）。别按版本号大小清理数据包。

**四、广告打开的是两个库**，`core/paths.py` 里只登记了一个；
`page2_decision_ext.sqlite` 的路径写在 `modules/ads/data.py`，因为契约禁止模块改 `core/`。

**五、`06-Pi-Agent交互Demo/src/competitor-agent.ts`（v1）不能当死代码删** ——
v2 从它 `import type` 九个类型。

---

## 六、验证纪律

改完代码，**先跑门禁再说做完了**：

```
/usr/bin/python3 09-工作台/tests/test_gates.py        # 24 条，必须全绿
```

**门禁全绿不等于页面没坏。** 柱子塌成 0×0、数字挤在一起、中文竖排、颜色没生效这类
只有看截图才抓得到——G11 就是专门留给人工的那一条。改了视觉就去看一眼真页面。

给数据界面加回归门禁时，**门禁必须打在真有那个特征的对象上**。
没有检查对象的门禁比没有门禁更糟，它给出虚假的安全感。
